apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: reservation-service-jwt-required
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      app: reservation-service
  action: ALLOW
  rules:

  # Rule 1: Allow Prometheus to scrape metrics endpoint
  - from:
    - source:
        principals:
        - cluster.local/ns/monitoring/sa/monitoring-stack-kube-prom-prometheus
    to:
    - operation:
        paths: ["/metrics"]

  # Rule 2: Service-to-service communication
  # Allow search-service to call reservation-service
  - from:
    - source:
        principals:
        - cluster.local/ns/{{ .Release.Namespace }}/sa/search-sa

  # Rule 3: Authenticated users for reservation endpoints
  - to:
    - operation:
        methods: ["GET", "POST", "PUT", "DELETE"]
        paths:
        - "/api/reservations/*"
        - "/api/reservations"
    when:
    - key: request.headers[x-auth-user-id]
      values: ["*"]
