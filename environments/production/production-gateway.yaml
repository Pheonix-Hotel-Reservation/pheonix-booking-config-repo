apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: production-gateway
  namespace: istio-ingress
spec:
  selector:
    istio: ingress
  servers:
  - port:
      number: 80
      name: http-prod
      protocol: HTTP
    hosts:
      - "prod.phoenix-project.online" # Include root/simplified production host
    tls:
      httpsRedirect: true
  - port:
      number: 443
      name: https-prod
      protocol: HTTPS
    hosts:
      - "prod.phoenix-project.online" # Wildcard cert is required here
    tls:
      mode: SIMPLE
      credentialName: phoenix-project-online-tls # Uses your existing wildcard certificate
