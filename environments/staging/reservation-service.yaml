# /environments/staging/reservation-service.yaml
image:
  tag: "@sha256:f49ec566e34de7d4622d6ae3834aaa19373fa7ad858a17fe25f76fb2576d4d3c"

imagePullSecret:
  name: harbor-pull-secret

rabbitmq:
  # This variable defines the SECRET that holds the app credentials (the admin password)
  secretName: "phoenix-staging-rabbitmq-rabbitmq-broker"
  # This variable defines the specific KEY within that secret
  passwordKey: "password"
  # The username is static based on the broker's auto-creation
  username: "admin"


replicaCount: 1
debugMode: true
